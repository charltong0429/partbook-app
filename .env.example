# Server-only. Shared secret used by api/findings.js to call Differ Market
# Intel's read-only partner API (GET /api/external/findings). Must match the
# value configured on the Differ Market Intel Vercel project. Never expose
# this to the browser bundle or commit a real value to git.
PARTNER_READ_SECRET=
