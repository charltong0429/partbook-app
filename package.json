{
  "name": "taurisweft-preview",
  "version": "0.1.0",
  "private": true,
  "description": "TaurisWeft brand and product preview",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs && node --check app.js && node --check about.js && node --check lib/draft-engine.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
